name: Docker Build

on:
  workflow_call:
#    inputs:
#      BUILD_TARGET:
#        type: string
#        required: true
#      NODE_ENV:
#        type: string
#        required: true
#      VITE_MODE:
#        type: string
#        required: true
#      SERVER_MODE:
#        type: string
#        required: true
#      SERVER_PORT:
#        type: string
#        required: true
#      SERVER_COOKIE_DOMAIN:
#        type: string
#        required: true
#
#    secrets:
#      NPM_TOKEN:
#        required: false
#      FORTAWESOME_TOKEN:
#        required: false
#      SERVER_SESSION_SECRET_KEY:
#        required: false
#      SERVER_KEYGRIP_KEY_A:
#        required: false
#      SERVER_KEYGRIP_KEY_B:
#        required: false
#      SERVER_KEYGRIP_KEY_C:
#        required: false

jobs:
  deployment:
    name: ${{ inputs.BUILD_TARGET }} ${{ inputs.NODE_ENV }} ${{ inputs.SERVER_MODE }}
    timeout-minutes: 10
    runs-on: ubuntu-latest
    environment:
      name: development
      url: github.com
    steps:
      - name: checkout actions
        uses: actions/checkout@master

      - name: start containers
        run: docker compose -f "docker-compose.yml" up --build
        env:
          BUILD_TARGET: ${{ inputs.BUILD_TARGET }}
          NODE_ENV: ${{ inputs.NODE_ENV }}

          SERVER_MODE: ${{ inputs.SERVER_MODE }}
          SERVER_PORT: ${{ inputs.SERVER_PORT }}
          SERVER_COOKIE_DOMAIN: ${{ inputs.SERVER_COOKIE_DOMAIN }}

          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          FORTAWESOME_TOKEN: ${{ secrets.FORTAWESOME_TOKEN }}

          SERVER_SESSION_SECRET_KEY: ${{ secrets.SERVER_SESSION_SECRET_KEY }}
          SERVER_KEYGRIP_KEY_A: ${{ secrets.SERVER_KEYGRIP_KEY_A }}
          SERVER_KEYGRIP_KEY_B: ${{ secrets.SERVER_KEYGRIP_KEY_B }}
          SERVER_KEYGRIP_KEY_C: ${{ secrets.SERVER_KEYGRIP_KEY_C }}

        continue-on-error: false

      - name: stop containers
        if: always()
        run: docker compose -f "docker-compose.yml" down